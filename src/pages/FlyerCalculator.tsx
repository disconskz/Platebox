import LeafletCalculator from "./LeafletCalculator";

/** Доработка 37 — шаблон «Флаер» (использует общий движок листовки). */
export default function FlyerCalculator() {
  return <LeafletCalculator mode="flyer" />;
}