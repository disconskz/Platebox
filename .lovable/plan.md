## Цель
Удалить шаг 3 «Допечатные» из калькулятора. Останется 6 шагов вместо 7.

## Изменения

### 1. `src/components/calc/Stepper.tsx`
Удалить элемент `"Допечатные"` из массива `STEPS`. Сетка mobile-индикатора `grid-cols-7` → `grid-cols-6`.

### 2. `src/pages/Calculator.tsx`
- Удалить блок `{step === 3 && (...)}` (стр. 1148–1176) целиком, включая поля `designQty` и ручные `manualForms`/`manualSetup` для стикеров.
- Сдвинуть нумерацию оставшихся шагов: `4→3`, `5→4`, `6→5`, `7→6`. Обновить заголовки внутри `Card` («4. …» → «3. …» и т.д.).
- Заменить все `step === 7` (граница навигации, кнопка «Далее», maxReached) на `step === 6`. Заменить `step >= 4/5/6/7` в `stepError` на новые номера.
- Удалить state `designQty`, `setDesignQty`, `manualForms`, `manualSetup`, `setManualForms`, `setManualSetup` и связанные импорты.
- В `calcInput` оставить `designQty: 2` (хардкод по умолчанию) и `manualForms`/`manualSetupSheets` не передавать (движок возьмёт авто).

### 3. Engine (`src/lib/calc/engine.ts`, `types.ts`)
Не трогать. Поля остаются опциональными в `CalcInput`; просто не передаются из UI.

## Что НЕ меняем
- Логика расчёта допечати (формы × 500 ₸, дизайн × 500 ₸) — остаётся в движке с `designQty=2` по умолчанию.
- Сохранение/PDF — без изменений.

## Проверка
- 49 тестов должны пройти (движок не тронут).
- Stepper показывает 6 шагов; навигация Назад/Далее работает; на шаге 6 кнопка «Далее» отключена.
- Расчёт стикеров даёт те же цифры (без ручных override).
